import React, { Component } from 'react'

import {FormGenerator} from '../index'

class Main extends Component {
    render() {
        return(
            <main>
                <FormGenerator />
            </main>
        )
    }
}

export default Main